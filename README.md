I small utilities for my personal use.

## Tools

- `gitscan`: Recursively scans directories to inspect git status and repository state.
- `is-published`: Determines if the current folder (or specified path) is a published crate, and if so, whether it is up to date with the registry.
